import ModalExample from "!raw-loader!headlessui-react-native-example-ui/src/modal/modal-example";
import React from "react";
export const CodeBlock = ({ code }) => (
  <pre>
    <code>{code}</code>
  </pre>
);

<CodeBlock code={ModalExample} />
